import React from "react";
import { Button, Input } from "antd";

const Home = () => {
  return (
    <div>
      <h2> Home page</h2>
      <Input />
      <Button type="danger">Button</Button>
    </div>
  );
};

export default Home;
